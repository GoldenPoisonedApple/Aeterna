services:
  # 現状フロントエンドのみ
  frontend:
    build: ./frontend
    container_name: aeterna_frontend
    restart: unless-stopped
    networks:
      - cf_tunnel_net

  # --- Future Backend ---
  # 将来の実装イメージ
  # backend:
  #   build: ./backend
  #   container_name: aeterna_backend
  #   networks:
  #     - cf_tunnel_net
  #   environment:
  #     - DATABASE_URL=...

  # db:
  #   image: postgres:16-alpine
  #   container_name: aeterna_db
  #   networks:
  #     - cf_tunnel_net
  #   volumes:
  #     - db_data:/var/lib/postgresql/data

networks:
  cf_tunnel_net:
    external: true

# 将来DBを使用する場合のみコメントアウト解除
# volumes:
#   db_data: